<?php
// Copy this file to config.php and fill in real values.
// config.php is git-ignored and must never be committed (m0t.AUTH.1.3 / m0t.BUILDER.4.3).
// On Hostinger: create the DB in hPanel, run schema.sql against it, then upload the
// real config.php to public_html/admin-api/config.php via FTP/File Manager — once.

define('DB_HOST', 'localhost');
define('DB_NAME', 'obvius_portfolio');
define('DB_USER', 'your_db_user');
define('DB_PASS', 'your_db_password');
